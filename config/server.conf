port = 8080              # Listen port
root_dir = ./www         # Static files directory
enable_logging = 1       # Enable request logging

# Routing rules
route GET / ./www                    # Static files
route GET /api http://backend:3000   # Reverse proxy